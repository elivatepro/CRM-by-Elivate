FROM node:22-alpine

WORKDIR /app

# The intake service is dependency-free: Node's built-in http and fetch only.
COPY intake/ /app/intake/

ENV NODE_ENV=production
ENV PORT=4000

EXPOSE 4000

USER node

CMD ["node", "/app/intake/server.mjs"]
